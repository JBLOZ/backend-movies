from setuptools import setup, find_packages

setup(
    name="utils",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        "logging",
    ],
    description="Módulo de utilidades para el proyecto backend-jbl42",
    author="JBL42",
)